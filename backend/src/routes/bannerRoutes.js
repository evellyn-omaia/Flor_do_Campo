const router=require("express").Router();const controller=require("../controllers/bannerController");const autenticar=require("../middlewares/authMiddleware");const verificarAdmin=require("../middlewares/verificarAdmin");router.get("/",controller.listar);router.post("/",autenticar,verificarAdmin,controller.criar);router.put("/:id",autenticar,verificarAdmin,controller.atualizar);module.exports=router;
